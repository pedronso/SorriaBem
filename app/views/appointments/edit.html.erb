<!DOCTYPE html>
<html>
<head>
  <title>Consultório Odontológico Sorria Bem - Editar Consulta</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <style>
      body {
          font-family: Arial, sans-serif;
          background-color: #f0f0f0;
          margin: 0;
          padding: 0;
      }

      header {
          background-color: #007bff;
          color: #fff;
          text-align: center;
          padding: 1rem 0;
      }

      .container {
          max-width: 800px;
          margin: 0 auto;
          padding: 2rem;
      }

      h1 {
          font-size: 2.5rem;
          margin-bottom: 2rem;
      }

      .btn {
          display: inline-block;
          padding: 1rem 1rem;
          background-color: #007bff;
          color: #fff;
          text-decoration: none;
          border-radius: 5px;
          font-size: 1rem;
          transition: background-color 0.3s ease;
          cursor: pointer;
      }

      .btn-primary:hover {
          background-color: #0056b3;
      }

      .patient-card {
          background-color: #fff;
          border: 1px solid #ccc;
          border-radius: 5px;
          padding: 1rem;
          margin-bottom: 1rem;
      }

      .patient-name {
          font-size: 1.5rem;
          font-weight: bold;
      }

      .patient-info {
          font-size: 1.2rem;
          color: #555;
      }

      /* Estilos para o formulário */
      .patient-form {
          margin-top: 2rem;
      }

      .appointment-form label {
          display: block;
      }

      .appointment-form input[type="text"],
      .appointment-form input[type="submit"] {
          width: 100%;
          padding: 0.5rem;
          font-size: 1rem;
          margin-bottom: 1rem;
          border: 1px solid #ccc;
          border-radius: 5px;
      }

      .appointment-form input[type="submit"] {
          background-color: #007bff;
          color: #fff;
          border: none;
          padding: 1rem 2rem;
          font-size: 1.2rem;
      }

      .appointment-form input[type="submit"]:hover {
          background-color: #0056b3;
      }
  </style>
</head>
<body>
<header>
  <h1>Consultório Odontológico Sorria Bem - Editar Consulta</h1>
</header>
<div class="container">
  <%= render "form", appointment: @appointment %>
  <%= link_to "Exibir esta consulta", @appointment, class: 'btn btn-primary' %>
  <%= link_to "Voltar para consultas", appointments_path, class: 'btn btn-primary' %>
</div>
</body>
</html>
